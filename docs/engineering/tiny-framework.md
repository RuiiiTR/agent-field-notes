# A tiny agent framework

The first version will contain only:

```text
LLMClient
Message
Tool
ToolRegistry
AgentState
Agent
RunResult
```

Features are added only when an experiment demonstrates a concrete need.
