# Does reflection help?

Reflection adds another model pass, but more inference does not automatically mean a better result.

The experiment will compare:

- a single-pass baseline;
- one bounded critic-and-revision pass;
- two reflection passes;
- cost and latency alongside task success.
